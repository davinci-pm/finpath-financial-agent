# FinPath 全流程测试报告（2026-08-30）

> 本文是修复前的基线审计，列出的 F-01～F-17 已在同日优化中完成修复。最终结果见 `PRELAUNCH_OPTIMIZATION_REPORT_2026-08-30.md`。

## 结论

FinPath 的两条 Demo 核心闭环、桌面端 11 个页面、生产构建和已有基础测试均能运行，但当前版本不建议直接面向真实用户上线。

主要原因不是“页面打不开”，而是若干输入校验、金融规则语义、文件确认和移动端交互问题会让用户看到或保存与其真实选择不一致的数据。测试已将这些缺口固化为预期失败回归；业务修复后，对应测试会提示“意外通过”，届时应移除 `test.fail` / `it.fails` 标记。

## 本轮执行结果

| 检查项 | 结果 |
|---|---:|
| ESLint | 通过 |
| TypeScript strict | 通过 |
| Vitest | 383 通过，3 个已知缺口按预期失败（共 386） |
| Playwright Chromium | 96 个用例全部完成，其中 22 个用例用于稳定复现已知缺口 |
| Next.js 生产构建 | 通过，13 个页面路由与 16 个 API 路由成功构建 |
| 规则组合 | 覆盖 288 组期限、应急金、负债、波动容忍、收入稳定性组合 |
| 桌面视口 | 1440×960 截图、1280×800 溢出检查通过 |

合计执行 482 个测试场景：457 个满足当前验收，25 个稳定复现已知缺口（部分缺口同时有单元与端到端两层复现）。

## 已验证通过的主流程

1. 首页提问 → 创建诊断 → 逐题澄清 → 生成资金计划。
2. 资金计划三桶合计 100%，输出经过证券代码、收益承诺和比例区间校验。
3. 资金计划 → 保存任务 → 任务中心 → 任务详情 → 完成步骤并更新进度。
4. PDF/PNG/JPG 上传 → 分析 → 确认字段 → 生成产品解读 → 保存任务。
5. 新增资产 → 资金地图总资产聚合更新；金额隐藏不改变 API 数据。
6. 任务状态筛选、学习题即时反馈、办事路线展开与清单切换。
7. 核心页面没有未捕获的浏览器异常或 `console.error`。
8. 非法 JSON、超长文本、负数/小数金额、超大文件、未知资源等基础边界能够返回 4xx。

## 缺陷清单

### P1：上线前必须修复

| ID | 缺陷 | 影响 |
|---|---|---|
| F-01 | 用户把澄清题全部“跳过”后，`skipped` 被强转成非法枚举或 `NaN` | 规则会错误声称“3 年以上期限”“应急储备充足”，属于金融建议事实失真 |
| F-02 | 未完成五道澄清题即可直接调用生成计划接口 | 可绕过产品要求的关键条件确认 |
| F-03 | 澄清 API 只校验字段名，不校验答案是否属于当前题选项 | 任意字符串可进入规则引擎并污染建议 |
| F-04 | 上传页编辑后的识别字段没有回传父组件 | 页面显示已修改，但最终解读仍使用旧值，用户确认失效 |
| F-05 | 文件类型校验使用“扩展名或 MIME 任一合法”，且接受空文件 | 改名或伪造 MIME 即可绕过类型校验，空内容也被存储 |
| F-06 | 未分析文档直接确认返回 500；分析后 `{ confirmed: {} }` 也能把文档标记为 ready | 文档状态机和最终报告前置条件可被绕过 |

### P2：建议上线前修复

| ID | 缺陷 | 影响 |
|---|---|---|
| F-07 | 收入稳定性虽被询问，但完全不影响规则输出 | 用户多回答一道题却得不到任何规则差异，解释链不完整 |
| F-08 | 资产 PATCH 只校验补丁，不校验合并后的完整记录 | 可产生金额下限大于上限的数据 |
| F-09 | 任务可直接标为 completed，但步骤和进度仍为未完成 | P10/P11 状态与进度不一致 |
| F-10 | `/ask` 仍是“阶段 1 占位”页 | 侧栏“问 AI”和“新建金融任务”进入不可用页面 |
| F-11 | 资金地图顶部 AI 输入框没有绑定提交动作 | 控件可输入、可点击，但没有任何结果 |
| F-12 | 资产表单在保存请求结束前关闭，后端校验错误不可见 | 倒置金额区间失败后用户不知道原因，也无法原地修正 |
| F-13 | 从资产切换到负债时使用旧类别闭包 | 可保存出“负债 + 定期存款”之类不一致组合 |
| F-14 | 390px 屏幕仍保留固定 220px 侧栏 | 正文只剩约 170px，标题、卡片和操作严重挤压 |
| F-15 | P02 的 `radiogroup` 不支持方向键，摘要铅笔按钮无动作 | 键盘可访问性和“返回修改不得丢失答案”验收未完成 |
| F-16 | 学习答案与进度只存组件状态，刷新即丢失 | P06“学习进度可保存”未完成 |
| F-17 | P07 费用与备用方案首屏常驻 | 与“通过二级入口查看”的信息层级验收不一致 |

## 本轮新增测试文件

- `e2e/api-boundaries.spec.ts`：API 错误码、非法输入、状态机、上传边界和数据一致性。
- `e2e/ux-regression.spec.ts`：控制台异常、首页输入、键盘操作、字段编辑、资产抽屉、学习、路线、任务筛选和移动端。
- `tests/server/combinatorial-rules.test.ts`：288 组金融规则组合与跳过/收入稳定性缺口。
- `tests/server/input-boundaries.test.ts`：资产、任务和文件输入边界矩阵。

## 未覆盖的外部环境

本轮运行的是项目 Demo 模式和 Mock AI Provider，没有消耗真实模型 Token。以下项目需要在修复 P1/P2 后进入单独的上线验收：

- 真实 Supabase 登录、RLS、数据库迁移和 Storage 权限。
- DeepSeek/OpenAI 超时、限流、错误输出和费用控制。
- 正式域名下的部署环境变量、CSP、日志、告警和回滚。
- 真机 Safari/Chrome、弱网、断网恢复与大文件上传。
- Lighthouse/Core Web Vitals 和真实生产数据量下的性能。
